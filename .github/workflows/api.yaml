name: API

on:
  push:
    paths:
      - ".github/**"
      - "api/**"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./api

    steps:
      - uses: actions/checkout@v4
      - uses: erlef/setup-beam@v1.17
        with:
          elixir-version: "1.17"
          otp-version: "27.0"
      - run: mix deps.get --check
      #- run: mix compile --all-warnings --no-deps-check --warnings-as-errors
      - run: ls -la
      - run: mix compile --all-warnings
      - uses: actions/upload-artifact@v4
        with:
          name: deps
          if-no-files-found: error
          path: deps

  test:
    name: Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./api
    needs: build

    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: deps
          path: deps
      - run: mix test --all-warnings --no-deps-check --warnings-as-errors

  credo:
    name: Credo
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./api
    needs: build

    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: deps
          path: deps
      - run: mix credo -a --strict

  dialyzer:
    name: Dialyzer
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./api
    needs: build

    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: deps
          path: deps
      - run: mix dialyzer
